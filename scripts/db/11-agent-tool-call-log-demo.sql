-- 【已废弃】勿再导入本文件。
-- 早期为联调统计页塞入的假数据（run_demo_*），与真实 Agent 调用无关。
-- 请执行 18-clear-tool-call-log-demo.sql 清理；真实日志由 Agent 检索（search_knowledge）自动写入。
--
-- 保留空文件说明，避免误跑旧脚本。
SELECT '11-agent-tool-call-log-demo.sql is deprecated; run 18-clear-tool-call-log-demo.sql instead' AS notice;
