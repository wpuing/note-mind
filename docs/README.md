# Docs（公仓可见）

本目录仅包含可公开的架构与部署说明：

- `architecture/` 架构总览与企业级说明书
- `api/` 接口规划
- `rag/` RAG 管线说明
- `deploy/` 本地部署

以下内容**故意不随公仓分发**（含密钥配置、内部开工流程、变更细节、方案补充稿等）：

- `plan/`（开工手册、开发计划）
- `changelog/`
- `castom/`

模型与服务密钥请只放在本地 `.env`，模板见仓库根目录 `.env.example`。
